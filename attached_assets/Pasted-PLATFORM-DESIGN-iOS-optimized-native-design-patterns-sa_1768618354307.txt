PLATFORM & DESIGN
- iOS-optimized native design patterns + safe area handling for all iPhone sizes
- Dark-mode-first premium theme (AMOLED black, subtle accents)
- SF Pro system font with strong hierarchy
- Smooth micro-animations + iOS haptic feedback (accept ride, start trip, complete trip, alerts)
- iOS gestures: swipe back, pull to refresh, long press for quick actions
- Use iOS-style bottom sheets for key actions and details

ONBOARDING & DRIVER STATUS
- Premium onboarding with simple steps:
  1) Permissions (location, notifications)
  2) Driver profile setup (mock)
  3) Vehicle selection (mock)
- Main status toggle: OFFLINE / ONLINE (large, prominent)
- “Go Online” flow with confirmation and haptic feedback
- While online, show “searching for trips” state with premium animation

HOME / DRIVER MAP (CORE)
- Map centered on driver location with smooth panning/zoom
- Driver marker with smooth heading rotation
- Heatmap overlay (mocked) showing demand zones (update every 15 min)
- Supply indicators (mocked): nearby drivers density
- Smart zone suggestions panel:
  - “Move to Zone A (high demand in 8 min)”
  - “Avoid Zone B (oversupplied)”
  - “Stay put (likely pickup within 5 min)”
- One-tap navigation button to suggested hotspot zone

TRIP REQUEST INBOX
- Incoming trip request card (full-screen / bottom sheet):
  - Pickup distance/time
  - Dropoff direction + approximate duration (show map snippet)
  - Estimated earnings (transparent)
  - Rider rating
  - Vehicle tier requested
  - Timer countdown with accept/decline
- Premium accept/decline animations + haptic feedback
- Auto-expire state if not accepted

ACTIVE TRIP FLOW
- States:
  1) Accepted → Navigate to pickup
  2) Arrived (tap “Arrived”)
  3) Start Trip (tap “Start”)
  4) In Trip (live route + ETA)
  5) Complete Trip (tap “Complete”)
- In-trip screen shows:
  - Rider name + rating (mock)
  - Pickup/dropoff details
  - ETA and route preview
  - Quick actions bottom sheet:
    - Message rider (mock UI)
    - Call rider (mock UI)
    - Safety tools
- Smooth driver marker animation, route line, and ETA updates (mock)

EARNINGS (HIGH PRIORITY SCREEN)
- Earnings dashboard:
  - Today / Week / Month tabs
  - Total earnings, platform fee, tips breakdown
  - Trips count, online hours, earnings/hour
- Trip receipts list with detailed breakdown per trip
- “Goal mode” (optional mock):
  - Set daily goal, progress ring

SMART INTELLIGENCE (KEY DIFFERENTIATOR)
- “Driver Autopilot” mode (UI only at first):
  - When enabled, app recommends best zones and timing
- Prediction cards (mock logic):
  - Demand rising/falling
  - Suggested reposition zones
  - Surge likelihood indicator (mocked)
- Explainability mini text:
  - “Based on time + recent pickups + zone flow”

SCHEDULING & PLANNING (OPTIONAL BUT PREMIUM)
- “Plan shift” screen:
  - Choose work hours (iOS picker)
  - Shows predicted busy windows (mock chart)
- Reminders: “High demand starting soon”

SAFETY
- Safety center:
  - SOS button
  - Emergency contacts
  - Share live trip (mock)
- Route deviation alert UI (mock)
- “Silent SOS” UI option (mock)

PROFILE & VEHICLE
- Driver profile screen:
  - Name/photo (mock)
  - Rating, trips completed (mock)
  - Vehicle info and documents placeholders (no real upload required for now)
- Preferences:
  - Trip types to accept (short/long, airport)
  - Navigation provider selection (Apple Maps/Google Maps buttons)

NOTIFICATIONS & LIVE STATES
- Push notification UI states (mock):
  - New request
  - Rider canceled
  - Surge/demand alert
- Offline/online persistence (local state)

IMPLEMENTATION NOTES
- Use mock data + mock timers for:
  - driver movement
  - trip requests
  - heatmap refresh every 15 minutes
  - earnings updates after trip completion
- Structure code cleanly so real APIs can replace mocks later.
- Focus on premium UI polish, animations, and realistic state management.